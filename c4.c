#include "helpers.h"
#include <stdio.h>

int main() {
	FILE *fp;
	char line[60];
	fp = fopen("4.txt", "r");
	
	while ()
    if (single_byte_XOR_solver(line, 60) != line) {
			/* print solution!*/
    }
  }
}
